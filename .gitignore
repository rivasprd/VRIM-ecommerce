# Dependencies
node_modules/

# Environment and secrets (never commit)
.env
.env.local
.env.*.local
.env.old
*.env.old
service-account.json
*.pem

# Root-level docs generated by AI / flow-specific (keep frontend/README.md if needed)
/*.md

# IDE / editor
.cursor/
.idea/
.vscode/
*.log

# AI / flow-specific (instrucciones Copilot y similares)
.github/

# Build and runtime
.next/
out/
build/
dist/
coverage/
.vercel

# Backups
*.old

# OS
.DS_Store
Thumbs.db

# Lockfiles optional: uncomment to ignore (some teams track them)
# package-lock.json
