# Server
PORT=8080

# Mercadopago (get from https://www.mercadopago.com/developers/panel/app)
MERCADOPAGO_ACCESS_TOKEN=your_access_token_here

# Optional: override back URLs for Mercadopago redirect (defaults: localhost:3000)
# BACK_URL_SUCCESS=http://localhost:3000/success
# BACK_URL_FAILURE=http://localhost:3000/failure
# BACK_URL_PENDING=http://localhost:3000/pending

# Optional: Firebase/Firestore. If not set, products and orders use in-memory mock data.
# Option A: path to service account JSON file
# GOOGLE_APPLICATION_CREDENTIALS=./service-account.json
# Option B: service account JSON as string (e.g. for Cloud Run)
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}
